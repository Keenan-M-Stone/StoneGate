# StoneGate SDK (generated)

This directory is generated by `tools/generate_stonegate_sdk.py`.

## Install (editable)

```bash
python3 -m pip install -e .
```

## Use

```python
import stonegate_api as sg
import stonegate_qec as qec

sg.WS_URL = "ws://localhost:8080/status"
```
